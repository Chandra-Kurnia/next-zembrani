import { Fragment } from "react";
import { useRouter } from "next/router";

const Show = () => {
    const {query} = useRouter();
    return (
        <Fragment>
            
        </Fragment>
    );
}

export default Show;